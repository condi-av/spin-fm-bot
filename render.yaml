services:
  - type: web
    name: spin-fm-bot
    env: docker          # ← главное изменение
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: BOT_TOKEN
        sync: false
